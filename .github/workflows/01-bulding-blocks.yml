name: 01 - Bulding Block

on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Say HEllo
              run: echo "Hello World!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed Step
              run: | #This mandatory to define multi line script
                echo "I will fail"
                exit 1
            - name: Say goodbye
              run: echo "Good Bye!" 